'use strict';

angular.module('app')
  .controller('SettingsProfileIndexCtrl', [
    '$q', '$scope', '$state',
    function ($q, $scope, $state) {

    // view model
    $scope.vm = {
    };

    $scope.save = function() {

    };

    function init() {

    };

    init();

  }]);
